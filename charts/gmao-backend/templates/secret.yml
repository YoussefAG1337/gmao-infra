apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gmao-backend.fullname" . }}-secret
  labels:
    {{- include "gmao-backend.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
