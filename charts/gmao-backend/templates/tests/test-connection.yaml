apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gmao-backend.fullname" . }}-test-connection"
  labels:
    {{- include "gmao-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gmao-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
